# Secrets
.env
.streamlit/secrets.toml

# Virtual environments
.venv/
venv/

# IDE
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
*.egg-info/

# NOTE: faiss_index/ is committed on purpose — it's the prebuilt index the
# deployed app loads (Streamlit Cloud disk is ephemeral). Rebuild it with
# `python ingest.py` and commit the result.
